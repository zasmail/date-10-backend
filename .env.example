# Rendered via machine/bw-inject.sh (bw://Folder/Item/field placeholders); the real .env stays gitignored.
DATABASE_URL=sqlite:///./database.db

# --- Added 2026-07-30: keys present in .env but missing from this template.
# --- Bitwarden migration — generate real .env with: ./machine/bw-inject.sh .env.example > .env
ANTHROPIC_API_KEY=bw://Dev-Secrets/Anthropic/api-key
TRIP_NINJA_AUTH_TOKEN=bw://Dev-Secrets/TripNinja/auth-token
